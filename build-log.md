# Build Log — arvindramachandran.tech
Read this file at the start of every session before touching code.

## Session 0 — 2026-08-29
- Built: Setup only. PRD reviewed; all §8 open decisions finalized (see below). No code yet.
- Key decisions (PRD §8, now closed — PRD to be treated as amended):
  - Typefaces: Space Grotesk (display) + IBM Plex Sans (body) + IBM Plex Mono (mono), self-hosted via @fontsource
  - Accent: amber/terminal orange #B45309 (hover #92400E) on off-white #FAFAF7 / off-black #141412
  - MED lives as homepage section (no /med page at v1)
  - Deploy target: Cloudflare Pages
  - Contact: mailto: only (no form)
  - Phone number: NOT shown on site (resume PDF only)
- Files touched: build-log.md (created)
- Known issues: none
- Next module: Module 1 — Project scaffold & design system

## Module 1 — Project scaffold & design system — 2026-08-29
- Built: Astro 5 + Tailwind v4 (@tailwindcss/vite) scaffold in portfolio/; design tokens in src/styles/global.css (Space Grotesk / IBM Plex Sans / IBM Plex Mono; paper/ink/muted/line/accent palette); single typed content module src/data/site.ts holding every PRD §6 fact (nav, hero, about, MED case-study data + architecture stages, secondary project, 6 skill groups, experience, education, SnowPro cert w/ credential ID S132356-260124-SOL); temporary placeholder index.astro exercising the tokens; favicon.svg = PAL sparkline-with-anomaly motif. Verified: npm run build (1 page) + astro check (0 errors/0 warnings). git repo initialized on main, no commits yet (commits await explicit request).
- Key decisions:
  - Tailwind v4 CSS-first config (@theme tokens in global.css, no tailwind.config.js) — deliberate escape from the default Tailwind look (PRD §3)
  - Fonts self-hosted via @fontsource packages; imports live in index.astro for now, move into Base.astro in Module 2
  - Nav semantics: "Work" anchors to the MED case-study section (#work); "Projects" anchors to secondary projects (#projects); MED CTA points to #work
  - astro check wired as `npm run check` for typechecking every module; Lighthouse/contrast/link audit deferred to Module 9
- Files touched: package.json, package-lock.json, astro.config.mjs, tsconfig.json, .gitignore, src/styles/global.css, src/data/site.ts, src/pages/index.astro, public/favicon.svg, build-log.md (appended)
- Known issues: /resume.pdf is a 404 until Module 8; MED repo URL + Medium article URL + Credly verify URL are TODO markers in site.ts; index.astro is a placeholder to be replaced by the Base layout in Module 2; site not yet visible anywhere (deploy comes with Cloudflare Pages setup later)
- Next module: Module 2 — Base layout & header

## Module 2 — Base layout & header — 2026-08-29
- Built: src/layouts/Base.astro (meta description/canonical/OG shell, theme-color, font imports moved here from the page, skip-to-content link, main landmark); src/components/Header.astro (sticky minimal text nav rendered from site.nav, PAL sparkline wordmark + name, persistent Resume button, accent underline on hover, aria-label="Main"); src/pages/index.astro re-rendered through Base (placeholder body kept for Module 3). Verified: npm run build (1 page) + astro check (0 errors/0 warnings) + dev-server HTML markers all present.
- Key decisions:
  - Baseline git commit created before this module per user request: f51ddd6 "Module 1: Astro scaffold, design tokens, site content module" (main branch)
  - No hamburger on mobile: 4 links + Resume button fit 375px via tightened gaps/padding (gap-x-2.5, px-4); wordmark name hidden under sm:, sparkline monogram stays
  - No scroll-spy / active-section JS — site keeps zero client JS
  - Footer landmark intentionally deferred to Module 8 (Base.astro gets the Footer import then)
- Files touched: src/layouts/Base.astro (new), src/components/Header.astro (new), src/pages/index.astro (rewritten), build-log.md (appended)
- Known issues: /resume.pdf still 404 until Module 8; og:image TODO comment in Base.astro until Module 8; Module 2 changes not yet committed (awaiting user go-ahead); 375/768/1440 visual check is a quick manual pass on the running dev server
- Next module: Module 3 — Hero

## Module 3 — Hero — 2026-08-29
- Built: src/components/Hero.astro (mono location kicker; Space Grotesk h1 name at 48/60/72px; positioning statement in display face; mono credibility stack row "Python · SQL · Databricks · AWS · LangGraph" — no pills; exactly 2 CTAs: solid amber "View MED" → #work + outline "Resume" → /resume.pdf; asymmetric md:grid with large PAL sparkline motif + "PAL — Price Anomaly Locator" mono caption on the right, hidden on mobile); index.astro now renders Base + Hero only; global.css gained :focus-visible accent outline (site-wide keyboard a11y). Verified: build + astro check clean (7 files, 0 errors) + dev-server HTML markers all present (h1, positioning, stack row, both CTAs, sparkline caption, location kicker).
- Key decisions:
  - Kicker uses factual location line from site.ts in mono muted — accent reserved for motif dot + CTAs (PRD §3 accent discipline)
  - PAL sparkline enlarged as hero-side personal mark (ties hero → MED narrative); aria-hidden, decorative only
  - Positioning statement set in display face at statement scale — typography-led per PRD §3, no imagery
  - Zero client JS maintained; no entrance animations
- Files touched: src/components/Hero.astro (new), src/pages/index.astro (rewritten), src/styles/global.css (focus-visible rule), build-log.md (appended)
- Known issues: /resume.pdf still 404 until Module 8; 375px hero relies on hidden sparkline + wrapping CTAs (visual pass recommended on dev server)
- Next module: Module 4 — About

## Module 4 — About — 2026-08-29
- Built: src/components/About.astro (editorial marker-column layout: mono h2 "About" label in a 180px left rail, 18px relaxed prose in a max-w-2xl column, hairline border-t section divider, id="about"); src/data/site.ts `about` restructured from a single string to `string[]` (two paragraphs: background/stack, then current MED focus + targeting — facts unchanged, split is presentational only); index.astro renders Hero + About. Verified: build + astro check clean (8 files, 0 errors) + dev-server markers all present (heading, both paragraphs, section id).
- Key decisions:
  - Section pattern established: hairline top border + mono small-caps-style label rail + content column — this becomes the template for Work/Projects/Experience/Contact sections (consistency without decoration)
  - Gap/non-CS background stated plainly inside the existing PRD prose — no extra apologia, per PRD §6.2
- Files touched: src/components/About.astro (new), src/data/site.ts (about: string → string[]), src/pages/index.astro, build-log.md (appended)
- Known issues: none new (/resume.pdf 404 unchanged)
- Next module: Module 5 — Projects: MED case study

## Content amendment — location & targeting copy — 2026-08-29
- Built: user-directed copy change, no layout/code changes. Hero kicker (site.location): "open to Singapore & GCC roles" → "open to Startup & GCC roles". About ¶2: "— Singapore FinTech & AI sector primarily, GCC roles in Chennai/Hyderabad/Bengaluru as a near-term step." → "— FinTech & AI sector primarily, Startup & GCC roles in Chennai/Hyderabad/Bengaluru as a near-term step." Verified: build + astro check clean; "Singapore" absent from src/ and served HTML.
- Key decisions: positioning re-aim per user (Singapore de-emphasized) — PRD §6.1/§6.2 wording now superseded; treat PRD as amended on this point.
- Files touched: src/data/site.ts, build-log.md (appended)
- Known issues: none new
- Next module: Module 5 — Projects: MED case study

## Module 5 — Projects: MED case study — 2026-08-29
- Built: src/components/MedCaseStudy.astro — the page centerpiece (PRD §6.3), section id="work" per nav semantics. Rail label "Work" (p, not heading — the project name is the section's real h2); honest status line "In progress — building in public" in mono; h2 project name in display face; framing hook "VIX measures fear. / PAL measures fragility." with the PAL sentence in accent (the one sanctioned PAL metric callout); description prose; **custom data-driven inline SVG architecture diagram** (LangGraph → Kafka → Databricks Lakehouse → Mosaic AI/RAG: 4 boxes + arrowhead markers, rendered from site.med.architecture with computed geometry, Tailwind token classes stroke-line/fill-ink/fill-muted, role="img" + aria-label, IBM Plex Mono text) — hidden below md; **mobile gets the same data as a stacked numbered list** with ↓ connectors (md:hidden); mono stack row "Stack — LangGraph · Kafka · Databricks · Delta Lake · Snowflake · FastAPI · Streamlit · Prometheus" (no pills); links row: "GitHub repo" accent link + Medium slot rendering "Medium deep-dive — coming soon" placeholder while articleUrl is null (auto-becomes a real link when set in site.ts). index.astro renders Hero + About + MedCaseStudy. Verified: build + astro check clean (9 files, 0 errors); 10/10 HTML markers present.
- Key decisions:
  - Diagram is data-driven from site.med.architecture (single-source-of-truth preserved); geometry computed in frontmatter
  - Mobile strategy: no horizontal scroll needed — SVG at md+, stacked list below md (readability over diagram fidelity at 375px)
  - Framing sentence split computed in frontmatter from the single framing string (no hardcoded copy in component)
  - SVG uses Tailwind theme utility classes (stroke-*/fill-*) so diagram colors stay tokenized
  - "Work" rail label demoted to <p> so the project name stays the section h2 (heading hierarchy)
- Files touched: src/components/MedCaseStudy.astro (new), src/pages/index.astro, build-log.md (appended)
- Known issues: MED repo link currently resolves to github.com/arvindram27 profile (TODO in site.ts until real repo URL provided); Medium slot intentional placeholder; dev server had died and was restarted this session
- Next module: Module 6 — Secondary project card + Skills

## Module 5a — Architecture diagram sizing tweak — 2026-08-29
- Built: user-requested size reduction of the MED architecture SVG. Geometry compacted (viewBox 920×120 → 764×88: boxes 200→170w/88→64h, label/detail fonts 16/12 → 13/11); removed min-w-[680px] + overflow-x-auto wrapper — svg is now `w-full max-w-[720px]`, so it always scales-to-fit its container with zero horizontal scroll at every md+ width and is capped at 720px on large desktops (~24% smaller visual footprint, ~83px tall vs ~127px). Mobile (<md) unchanged: stacked numbered list fits 375px with no scroll. Verified: build + astro check clean; new viewBox + classes present in built HTML; dev server up.
- Key decisions: scale-to-fit beats min-width+scroll for this diagram (worst-case md render still yields ~12px effective label text — readable)
- Files touched: src/components/MedCaseStudy.astro, build-log.md (appended)
- Known issues: none new
- Next module: Module 6 — Secondary project card + Skills

## Cloudflare agent setup — 2026-08-29
- Built: environment setup per Cloudflare's official agent instructions (developers.cloudflare.com/agent-setup/prompt.md), OpenCode branch. 13 Cloudflare skills installed globally to ~/.agents/skills/ (agents-sdk, cloudflare, cloudflare-email-service, cloudflare-one, cloudflare-one-migrations, durable-objects, sandbox-*, turnstile-spin, web-perf, workers-best-practices, wrangler) — opencode auto-loads them. 5 remote MCP servers registered in ~/.config/opencode/opencode.jsonc: cloudflare, cloudflare-docs, cloudflare-bindings, cloudflare-builds, cloudflare-observability (shapes validated against https://opencode.ai/config.json — oauth:{} is valid McpOAuthConfig). OAuth for the main `cloudflare` server completed successfully via `opencode mcp auth cloudflare`; docs server needs no auth; bindings/builds/observability trigger OAuth on first use.
- Key decisions: agent-side setup only — no project files touched; deploy wiring (Cloudflare Pages ← GitHub repo arvindram27/arvindramachandran-tech) happens in Module 9 using these MCPs/wrangler skill
- Files touched: ~/.agents/skills/* (global), ~/.config/opencode/opencode.jsonc (global), build-log.md (appended)
- Known issues: requires opencode restart for MCP servers to load; turnstile-spin skill flagged High Risk by Snyk (unused by this project — ignore unless Turnstile is ever needed)
- Next module: Module 6 — Secondary project card + Skills (pointer updated after Module 5 landed; entry written before Module 5)

## Module 6 — Secondary project card + Skills — 2026-08-29
- Built: src/components/SecondaryProjects.astro (section id="projects", rail h2 "Projects" — heading hierarchy restored to the About pattern; one bordered hairline card per entry in site.secondaryProjects — currently the Multi-Agent Market Intelligence System: h3 display name, muted description, accent "GitHub repo" link to github.com/arvindram27/multi-agent-market-intelligence; array-driven so future projects append without layout changes); src/components/Skills.astro (section id="skills", rail h2 "Skills", semantic <dl> with divide-y hairlines: dt = mono category label, dd = items joined " · " — plain text blocks mirroring the resume, zero progress bars/percentages per PRD §6.4; all 6 groups from site.skills). index.astro renders Hero → About → MedCaseStudy → SecondaryProjects → Skills. Verified: build + astro check clean (11 files, 0 errors); 9/9 HTML markers present; dev server restarted (died again between sessions).
- Key decisions:
  - Card = single hairline border, no shadow/rounded-bloat (PRD §3: not soft-UI); visual weight deliberately far below MED per PRD §6.3 "shorter treatment"
  - Skills as <dl> rows: category in the mono left column, items as body prose — resume-mirroring without decoration
  - No stack tags on the secondary project (PRD lists none — nothing invented)
- Files touched: src/components/SecondaryProjects.astro (new), src/components/Skills.astro (new), src/pages/index.astro, build-log.md (appended)
- Known issues: /resume.pdf 404 unchanged; MED repo URL TODO unchanged
- Next module: Module 7 — Experience + Education/Certifications

## Favicon set — cross-platform (desktop/iOS/Android) — 2026-08-29
- Built: full multi-size icon set generated from public/favicon.svg (the header PAL sparkline mark with baked-in colors) via new committed script scripts/build-favicons.mjs (`npm run favicons`, sharp + png-to-ico devDeps, 0 npm vulnerabilities). Generated: favicon.ico (16+32+48 embedded), favicon-16x16/32x32/48x48.png, apple-touch-icon.png (180, full-bleed — iOS rounds corners itself), android-chrome-192/512.png, android-chrome-maskable-192/512.png (artwork inset to 70% on paper background for adaptive-icon safe zones), site.webmanifest (name/short_name/start_url/display/theme_color #FAFAF7/background_color). Base.astro head now has the full link set: ico sizes="any" + SVG + 32/32 + 16/16 PNGs + apple-touch-icon + manifest. Verified: pixel dimensions of all 8 PNGs exact via sharp metadata; build + astro check clean (12 files, 0 errors); dev server serves all 10 assets with 200 + correct content types (manifest = application/manifest+json).
- Key decisions:
  - Rasterize from public/favicon.svg (not the header inline SVG) — same motif, colors baked, paper bg keeps it visible on dark tab bars
  - Maskable variants inset to 70% (Android adaptive masking crops ~20% edge)
  - Skipped browserconfig.xml/mstile (legacy Edge tiles — 2026 Edge reads the manifest)
  - short_name "Arvind R"; script top-comment mandates syncing manifest name with site.ts
- Files touched: scripts/build-favicons.mjs (new), public/{favicon.ico, favicon-16x16.png, favicon-32x32.png, favicon-48x48.png, apple-touch-icon.png, android-chrome-192x192.png, android-chrome-512x512.png, android-chrome-maskable-192x192.png, android-chrome-maskable-512x512.png, site.webmanifest} (generated), package.json (favicons script + devDeps), src/layouts/Base.astro, build-log.md (appended)
- Known issues: favicon.ico renders from PNG intermediates (fine at 16-48px); browser-tab/iOS/Android home-screen visual confirmation needs the user after deploy (favicons cache aggressively — hard-refresh); not yet committed
- Next module: Module 7 — Experience + Education/Certifications

## Module 7 — Journey timeline (replaces planned Experience module) — 2026-08-30
- Built: user-directed rebuild of the Experience plan as "Journey", a scroll-animated vertical timeline (PRD §6.5 superseded). Nav item 3 renamed Experience→Journey (position unchanged, others untouched; section id="journey"). src/components/Journey.astro: factual 2-sentence intro; 6 nodes from user-supplied biographical facts (2017 graduation + Civil Services/RBI Grade B/SEBI Grade A prep → capital-markets interest throughline → Kindle + Seth Godin's "The Dip" pivot → Midjourney generative-art phase → AI/ML-under-the-hood/agents → MED as proof node), each with weight low/medium/high controlling title/body type scale and marker size; node 4 embeds 3 real Midjourney images (optimized from user-provided Downloads files via sharp: 1200px max, mozjpeg q80 → journey-art-1/2/3.jpg, 149/174/229 KB, grid-cols-3 aspect-square object-cover, lazy+async); node 6 marker = amber filled dot (timeline "anomaly" — ties to PAL sparkline without reusing it) + MED preview placeholder (sparkline motif + visible TODO until real PAL charts exist); CTA after timeline = solid amber "View MED" → #work (same classes as hero primary). Animation: ~35-line inline vanilla script (first client JS on the site — PRD §4 sanctions minimal vanilla JS where truly needed): IntersectionObserver reveal per node (opacity 0→1, translateY 20px→0, 300ms ease-out, ≤240ms stagger, fire-once) + rAF-throttled scroll handler driving the track's ink fill line (grows only with scroll, never animates on load). prefers-reduced-motion: JS guard skips everything + CSS media query forces static visible; no-JS = static visible (hidden state added only by JS).
- Key decisions:
  - PRD amendments by user decision: resume experience entries (Independent Developer 2017–present, CK's Foods internship) REMOVED from site entirely — employment history lives on resume PDF only; PRD §9 "all resume facts reproduced" criterion amended accordingly; experience data + type deleted from site.ts
  - Vertical timeline on ALL breakpoints (mobile-native; no horizontal reflow)
  - Weight system (low/medium/high) implements the user's per-node visual-prominence spec without new styles — all typography/colors from existing tokens
  - Midjourney alts kept factual/descriptive; no artist statements invented
  - Fix during build: an edit accidentally renamed the Certification type to SiteContent (duplicate); caught and repaired before verification — Certification intact
- Files touched: src/data/site.ts (nav rename, experience removed, journey added), src/components/Journey.astro (new), src/pages/index.astro, public/journey/journey-art-{1,2,3}.jpg (new, optimized), build-log.md (appended)
- Known issues: MED preview is an intentional placeholder (TODO visible in UI) until PAL charts exist; scroll-reveal/line-fill/reduced-motion behavior needs a manual pass on the dev server (devtools can emulate reduced motion); Education & Certifications section (SnowPro credential ID etc.) still unbuilt — queued as Module 8 along with Contact/Footer
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Module 7a — Journey copy correction + typography consistency — 2026-08-30
- Built: user-directed copy + consistency pass on the Journey timeline, no animation/order/CTA changes. Copy: all 6 node bodies replaced with tightened user-supplied text (graduation 2017 → prep through 2025 — fact correction, node 2 era now "2017–2025"; "The exams didn't work out. The interest never left."; Kindle from sister + The Dip; Midjourney; LLMs/agents/orchestration; MED "one module at a time"); "The Dip" italicized via <em>. Typography: weight system simplified "low|medium|high" → "normal|emphasis" (type + data); body text now ONE class for all 6 nodes (mt-2 text-base leading-relaxed text-muted — kills text-sm/text-lg leftovers); titles uniform mt-1 text-lg font-semibold EXCEPT node 5 at text-2xl (the one agreed emphasis step); markers uniform h-3 w-3 except node 5 h-4 w-4 (top-1 nudge keeps both dot sizes centered on the line at x=6px); node 6 keeps amber anomaly dot but at standard size/title. Node bodies now render via set:html (data solely from our own typed site.ts module — enables inline <em>). Verified: build + astro check clean (13 files, 0 errors); all new copy fragments + <em>The Dip</em> present in built HTML; uniform body class occurs exactly 6×; 0 leftovers of old size variants.
- Key decisions: node 6 (MED) title intentionally returns to standard text-lg per "headers consistent, exception = node 5 only" rule (amber dot retained); set:html accepted on first-party content only
- Files touched: src/data/site.ts (nodes rewritten, weight type), src/components/Journey.astro (class maps, marker sizing, set:html), build-log.md (appended)
- Known issues: none new (MED preview placeholder + dev-server motion pass unchanged)
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Module 7b — Journey line draw synced to node reveals — 2026-08-30
- Built: user-directed animation refinement, script-only change in src/components/Journey.astro. Per-node IntersectionObserver reveal was already compliant (each node reveals on its own intersection, fire-once via unobserve, 300ms ease-out fade + 20px rise, not gated by previous nodes, once per node) — left untouched. The one gap fixed: the ink line previously grew via a continuous rAF scroll-progress handler (independent of reveals); it now extends in sync with reveals — on each node's reveal the fill height is set to that node's marker Y within the track (li.offsetTop; track top offset and marker center cancel at 12px), with Math.max monotonic growth so a fast scroll that reveals later nodes first can never shrink the line; the 300ms ease-out height transition makes line + node appear together as a unit. Removed the scroll listener entirely (resize listener recomputes the max revealed target). Reduced-motion: pre-existing JS guard + CSS unchanged, plus .journey-fill now forced to height 100% in the reduced-motion media query, and a <noscript> style does the same for no-JS (static timelines show the full ink line instead of only the faint guide track). Verified: build + astro check clean (13 files, 0 errors); old anchor-based scroll logic fully absent from built output; new markers (offsetTop/journey-visible/unobserve/IntersectionObserver) present in the inlined script; noscript + reduced-motion rules in built HTML.
- Key decisions: monotonic max for out-of-order fast-scroll reveals; faint bg-line guide track intentionally always visible (it IS the unrevealed state); first node needs no line above it (offsetTop 0 = marker already at track top)
- Files touched: src/components/Journey.astro (script block rewritten, reduced-motion CSS addition, noscript added), build-log.md (appended)
- Known issues: visual confirmation of per-node reveal pacing + reduced-motion emulation needs a manual scroll pass on the dev server
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Hero secondary CTA → social icon links — 2026-08-31
- Built: hero's secondary "Resume" button (next to View MED) replaced with two icon-only links — GitHub (github.com/arvindram27) and LinkedIn — rendered from new site.hero.socials data. Monochrome brand marks (standard GitHub octocat + LinkedIn glyph paths, fill=currentColor, 16px glyphs) per user choice over line-style icons; classes p-2 text-muted hover:text-ink (~32px tap target, no fill/border → clearly secondary to the solid amber View MED, hover mirrors nav's muted→ink); target="_blank" rel="noopener noreferrer" + aria-label on each; kept in the same mt-10 flex-wrap gap-4 container — identical spacing rhythm, wraps safely at 375px. Nav Resume button untouched. Data: new canonical `export const links` in site.ts (github + LinkedIn) — site.links spreads from it and hero.socials references it, so footer/Contact will render the same URLs.
- Key decisions: LinkedIn URL canonicalized to the hyphenated slug https://www.linkedin.com/in/arvind-ramachandran-r (user confirmed; PRD §6.7's arvindramachandran-r superseded — old slug verified absent from built HTML); ctas.secondary removed from the hero type (nav consumes site.resumePath directly, unaffected)
- Files touched: src/data/site.ts (links const, hero socials + type), src/components/Hero.astro (icon paths + socials map replacing Resume button), build-log.md (appended)
- Known issues: none new (verified /resume.pdf appears exactly once in built HTML — nav only; build + astro check clean)
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Hero social icon sizing tweak — 2026-08-31
- Built: user-requested ~20% enlargement of the hero GitHub/LinkedIn icons. Glyphs h-4 w-4 (16px) → h-5 w-5 (20px, nearest on-scale Tailwind step per user choice over exact 19.2px); link padding p-2 → p-1.5 so each link's total footprint stays exactly 32px — layout rhythm next to View MED and 375px wrap behavior unchanged, only the glyph grows. Hover/aria/target attrs untouched. Verified: build + astro check clean; h-5 glyphs + p-1.5 present in built HTML, old p-2 classes absent.
- Key decisions: on-scale 20px over arbitrary 19.2px; footprint-preserving padding rebalance honors the earlier "layout must not visually shift" directive
- Files touched: src/components/Hero.astro (two class changes), build-log.md (appended)
- Known issues: none
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Mobile nav — scrollable pill row — 2026-08-31
- Built: header restructured for <640px (sm breakpoint; ≥640px desktop/tablet DOM preserved exactly as before). Mobile: row 1 = sparkline monogram + FULL NAME (restored — was hidden below sm since Module 2; room exists now that links moved out) + Resume button fixed right; row 2 = horizontally scrollable pill row (Work/Projects/Journey/Contact) rendered from site.nav — overflow-x-auto + whitespace-nowrap (never wraps), scrollbar visually hidden via new global @utility no-scrollbar (scrollbar-width:none + ::-webkit-scrollbar display:none), pills = rounded-full hairline border-line + muted text + hover text-ink/border-ink/20 (understated, matches palette; no fills/glows). Edge fades: two pointer-events-none paper-gradient overlays (Tailwind v4 bg-linear-to-r/l) — right visible by default, left appears once scrolled; ~15-line vanilla script toggles opacity-0 from scroller.scrollLeft (passive listener + resize), consistent with the existing minimal-JS pattern; no-JS degradation = right fade only. Active-pill scroll-spy deliberately skipped (no scroll-spy exists — user requirement was conditional; noted as future option). Anchor-offset correctness: mobile sticky header grew 64→~112px, so scroll-mt-28 sm:scroll-mt-20 added to all 5 anchored sections (#about #work #projects #skills #journey; #contact inherits pattern in Module 8). Logo/Resume/nav duplicated per breakpoint row with sm:hidden / hidden sm:flex toggles (display:none removes hidden twins from a11y tree; mobile pills nav has no separate landmark — links are the same anchors).
- Key decisions: pill row strictly below sm per "mobile only / tablet unchanged"; full name restored on mobile (serves the stated logo+name-always-visible requirement); JS-driven conditional left fade over pure-CSS both-edge mask (spec-exact)
- Files touched: src/components/Header.astro (restructured: mobile row + desktop row + pill row + fade script), src/styles/global.css (no-scrollbar utility), About/MedCaseStudy/SecondaryProjects/Skills/Journey .astro (scroll-mt classes), build-log.md (appended)
- Known issues: manual pass needed at 375px (swipe pills, fade toggling, no wrap, anchor landing) — cannot be verified by build alone
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Mobile pill nav — decoupled + tuck-and-hide — 2026-08-31
- Built: pill row moved out of <header> into its own layer (Header.astro). Header unchanged visually, z-40→z-50; pill row now sibling sticky top-16 z-40 with own bg-paper — pins directly below the 64px header and slides BEHIND it (opaque header) when tucking via transform translateY(-110%), transition 250ms ease-in-out (minifier emits .25s). Transform-based hide → sticky row keeps its layout slot, zero reflow/jump; freed 48px strip shows page content scrolling beneath. Tuck-and-hide script (merged with fade script, rAF-throttled passive listener): scrollY ≤ 4 → always shown (hard top rule); deltas < 4px ignored (trivial-movement guard); scrolling down AND scrollY > 80 (midpoint of the 50–100px band, one constant) → tuck; scrolling up any amount → show immediately. Tucked state sets data-tucked="true" + inert attribute (keyboard focus can't land in the hidden row); show reverses both. prefers-reduced-motion: transition none (instant tuck/return, state logic unchanged). Desktop unaffected (row is sm:hidden; handler idles). Single border-b stays on the header (pinned chrome keeps one consistent hairline; pill row borderless).
- Key decisions: sticky-below-header layering (z-50 over z-40) instead of fixed positioning — no body padding compensation needed; 80px threshold constant; inert over aria-hidden (also blocks focus, not just AT)
- Files touched: src/components/Header.astro (structure move, z-index, tuck CSS, script), build-log.md (appended)
- Known issues: manual scroll pass needed at 375px (tuck after ~80px down-scroll, instant re-show on any up-scroll, top rule, no content jump) — build verifies logic presence only (translateY(-110%)/.25s/inert/80 all confirmed in built output)
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## Site-wide scroll-reveal (extends Journey pattern) — 2026-08-31
- Built: generic data-reveal system extending the Journey node animation site-wide. global.css: .reveal (opacity+transform 300ms ease-out transitions) / .reveal-hidden (opacity 0 + translateY(20px); [data-reveal="fade"] variant strips the transform) / .reveal-visible, all hand-written (immune to utility purging), plus prefers-reduced-motion block neutralizing hidden state + transitions — same treatment as Journey's. Base.astro: ONE site-wide IntersectionObserver (threshold 0.15, fire-once via unobserve — identical mechanics to Journey) over all [data-reveal] elements; hidden class applied by JS only → no-JS and reduced-motion render everything immediately visible; reveal swaps hidden→visible (avoids specificity clash with the fade-variant rule). Tagged: About whole grid (fade), MedCaseStudy header grid + SVG diagram + mobile diagram list (slide) + stack row + links row (fade), SecondaryProjects whole grid (slide), Skills whole grid (fade) — 8 units, 4 fade variants. EXCLUDED per spec: hero (renders instantly — verified first data-reveal occurs after hero markup), header, mobile pill nav, Journey (untouched — its scoped system still drives the line-fill sync). Contact (Module 8) inherits the pattern by adding attributes.
- Key decisions: new generic system rather than refactoring Journey's scoped implementation (its observer is load-bearing for the line-fill; refactor risk with zero visible gain); fade-only for short/simple blocks (About/Skills/stack/links), slide for heavier blocks (MED diagram, project cards) per delegated judgment; whole-grid units for short sections so headings are included, per-block units inside the tall MED case study
- Performance/listener census (verified in built output): IntersectionObserver instances = 2 (Journey + site-wide — IO is observation-based, not scroll-driven); page scroll listeners unchanged at 1 (pill-nav tuck) + 1 horizontal scroller listener (pill-nav edge fades) — the reveal system added ZERO scroll listeners, no conflict with tuck-and-hide. Effects are opacity/transform only — no display toggling, no layout impact, lazy images/data inside revealed blocks load and function normally.
- Files touched: src/styles/global.css (reveal classes), src/layouts/Base.astro (site-wide script), About/MedCaseStudy/SecondaryProjects/Skills .astro (data-reveal attributes), build-log.md (appended)
- Known issues: reveal pacing needs a manual eyeball pass (devtools reduced-motion emulation too); Journey verification markers unchanged. Post-write fix: the SecondaryProjects.astro edit initially reported success but did not persist (suspected OneDrive sync interference) — re-applied (a first re-application accidentally merged two container divs; caught via read-back and corrected), final state verified: 8 element tags + 1 script reference = 9 data-reveal occurrences in built HTML, all four sections confirmed tagged
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## PAL sparkline line-draw animation + mobile hero spacing — 2026-09-01
- Built: hero PAL sparkline now draws left-to-right on a 1000ms ease-out stroke reveal (SVG pathLength="1" + stroke-dasharray/dashoffset keyframes — no measurement, no charting lib), amber anomaly dot fades in at 820ms (the line reaches the dot's position at ~82% of path length ≈ 81% of ease-out duration, +10ms safety so it never pops early). SPEC AMENDMENT (user-directed, supersedes the original load-triggered/never-IO rule): mobile hero gained spacing (see below), so the chart may sit near/below the mobile fold — animation is now gated by ONE small IntersectionObserver in Hero.astro (threshold 0.3, fire-once): desktop/tablet chart is in the initial viewport → fires immediately on load (original behavior preserved there); mobile → fires when scrolled into view, so the animation is always seen. Arm-then-play pattern (pal-armed hides line+dot via dashoffset/opacity only when JS is active; pal-play starts keyframes) → no-JS = fully-drawn static chart + visible dot. prefers-reduced-motion: JS guard skips arming + CSS media query forces final drawn state (animation none !important). Mobile layout: aside no longer hidden below md — chart stacks directly below the CTA row (grid gap gap-8 md:gap-12, 32px rhythm; w-72 fits 375px, label legible); hero mobile bottom padding pb-16 → pb-24 per user's "more space between CTAs and About, don't shrink existing" (desktop sm values unchanged). Exclusions honored: only line+dot animate; all other hero content instant; Journey's decorative sparkline untouched. Independence: zero new scroll listeners (census still 2 = pill-nav tuck + pill-scroller fades); IO instances now 3 (Journey, site-wide reveal, PAL) — IO is observation-based, not scroll-driven. Verified in built output: pathLength/pal-line/pal-dot/data-pal-chart/armed+play logic/keyframes/820ms delay/reduced-motion overrides/pb-24/gap-8 all present; build + astro check clean (13 files, 0 errors).
- Key decisions: pure CSS keyframes with pathLength normalization (no getTotalLength measurement); IO-gated arm-then-play over pure load-trigger (spec amendment — animation always witnessed); 1000ms duration + 820ms dot delay derived from path-distance math; scoped Astro styles keep the system component-local
- Files touched: src/components/Hero.astro (aside always rendered + classes, scoped keyframes, IO script, spacing), build-log.md (appended)
- Known issues: animation playback + 3-width visibility (375/768/1440) need a manual pass — on very short mobile viewports (667px, e.g. iPhone SE) the chart bottom may need a sliver of scroll despite direct-below-CTA placement (no artificial gaps exist)
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder

## PAL sparkline trigger — scroll-triggered on mobile only — 2026-09-01
- Built: user-directed trigger fix. Root cause of "animation fires unseen on mobile": the chart bottom-edge peeks into taller phone viewports at load, so the previous IO (threshold 0.3) fired immediately and the draw finished before the user looked down. Hero.astro script is now breakpoint-gated at max-width 767px — the same md: boundary where the chart layout switches from beside-text to below-CTA (per user instruction to reuse the established breakpoint). Mobile (<768px): pal-armed → IntersectionObserver with rootMargin "0px 0px -25% 0px" + threshold 0 — the chart must rise above the bottom 25% viewport line to fire, so a bottom-edge peek at load no longer counts; fire-once via unobserve (no re-trigger on scroll-away-and-back). Desktop/tablet (≥768px): pal-armed → pal-play in the same tick, NO observer at all — genuinely load-triggered (stricter than the previous IO-at-load); keyframes' from-state applies on the first painted frame so there is no flash of the completed chart. Animation itself unchanged: 1000ms ease-out stroke reveal, 820ms dot. Reduced-motion guard unchanged (skips arming → static drawn chart); no-JS fallback unchanged. Zero changes to CSS/markup/chart position/spacing/other hero content; zero new scroll listeners (PAL IO now created only on mobile; IO count stays 3). Verified in built output: 767px gate, rootMargin -25%, old threshold 0.3 absent, both branches, reduced-motion guard; build + astro check clean (13 files, 0 errors).
- Key decisions: breakpoint-gated branches over a single observer (desktop gets true load-trigger with zero IO; mobile gets the entry-bar rootMargin — a bottom-edge peek ≠ "seen"); behavior chosen at load, not re-evaluated on rotation across 768px (edge case, consistent with pill-nav breakpoint stance)
- Files touched: src/components/Hero.astro (script block only), build-log.md (appended)
- Known issues: manual pass needed — 375px (no play at load even with edge peek; plays on scroll into view), 768/1440 (plays at load), reduced-motion static
- Next module: Module 8 — Education/Certifications + Contact/Footer + OG image + resume.pdf placeholder
